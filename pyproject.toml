[project]
name = "knowledge_app"
version = "0.1.0"
description = "Happy August!"
readme = "README.md"
authors = []
requires-python = ">=3.14"
dependencies = []

# [build-system]
# requires = ["uv_build>=0.12.1,<0.13.0"]
# build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
